name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Validate composer.json and composer.lock
      run: composer validate --strict -d app_rest

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: app_rest/vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress -d app_rest

    - name: Run phpcs
      run: composer run-script cs-check -d app_rest

    - name: Run test suite
      run: composer run-script test -d app_rest
